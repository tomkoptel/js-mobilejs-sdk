define 'js.mobile.ios.dashboard.callback', (require) ->
  class IosCallback
    onMaximize: (title) ->
      return

    onMinimize: ->
      return

    onScriptLoaded: ->
      return

    onLoadStart: ->
      return

    onLoadDone: ->
      return

    onLoadError: (error) ->
      return
